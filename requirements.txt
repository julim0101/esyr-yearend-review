# ESYR — 배포용 (Vercel 함수 용량 225MB 제한을 지킨다)
Flask==3.1.3
Flask-SQLAlchemy==3.1.1
PyMuPDF==1.26.3
pillow==11.3.0

# Postgres 드라이버 — 순수 파이썬이라 용량이 작다 (psycopg[binary]는 수십 MB)
pg8000==1.31.2

# OCR 은 Vercel 서버리스에서 동작하지 않는다 (시스템 바이너리 설치 불가).
# 로컬·컨테이너 실행 시에만 설치한다:  pip install pytesseract==0.3.13
