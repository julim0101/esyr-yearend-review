# 업로드 원본·DB — 절대 올리지 않는다 (개인정보)
instance/
*.sqlite3
*.db

# 생성물
samples/*.pdf
__pycache__/
*.py[cod]
.venv/
venv/
.env
.env.local

# OS
Thumbs.db
.DS_Store

# OCR 언어데이터 (용량 큼 — README 안내대로 내려받는다)
tessdata/

# ONNX OCR 검증용 모델 (정확도 미달로 채택하지 않음 — README 참고)
ocr_models/

.vercel
.env*

# CLI 임시 파일
supabase/.temp/
.env.local
